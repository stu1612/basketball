import React from 'react';
// import images
import header from '../img/header/header.jpg';

const Header = () => {
  return (
    <header>
      <img src={header} alt='' />
    </header>
  );
};

export default Header;
